name: Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests:
    permissions:
      contents: read
    uses: builtnoble/.github/.github/workflows/php-package-tests.yml@057258c6bbb173fc64da8f7b15d93fd837ced874 # v1
    with:
      php-extensions: mbstring
