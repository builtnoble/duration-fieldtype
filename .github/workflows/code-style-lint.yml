name: Lint code style issues

on:
  pull_request:

permissions: {}

jobs:
  lint:
    permissions:
      contents: read
    uses: builtnoble/.github/.github/workflows/php-code-style-lint.yml@v1
    with:
      php-extensions: mbstring
